/**
 * Files on disk for this fixture.
 *
 * The generator resolves match() rules against these paths, so every path
 * here is deliberate. Add a path to widen what a match rule can reach.
 */
const files: string[] = [
  "libs/search/config/deep/sample.json",
  "libs/search/config/sample.json",
  "libs/search/index.ts",
];

export default files;
