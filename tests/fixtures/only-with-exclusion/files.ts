/**
 * Files on disk for this fixture.
 *
 * The generator resolves only()/add() rules against these paths, so every path
 * here is deliberate. Add a path to widen what a rule can reach.
 */
const files: string[] = [
  "libs/air/index.ts",
  "libs/air/locales/en-US/air.json",
  "libs/air/locales/fr/air.json",
  "libs/stays/index.ts",
  "libs/stays/locales/de/stays.json",
  "libs/stays/locales/en-US/stays.json",
];

export default files;
